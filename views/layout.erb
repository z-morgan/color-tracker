<!DOCTYPE html>
<html>
  <head>
    <title>Salon Inventory App</title>
    <link rel="stylesheet" href="/stylesheets/app.css">
  </head>
  <body>
    <header>
      <h1>Salon Inventory Tracker</h1>
      <div class="menu">
        <nav>
          <ul>
            <li><a href="/inventories">My Inventories</a></li>
            <li id="signin-status">
              <% if session[:username] %>
                <form action="/signout" method="post">
                  <label>Signed in as <%= session[:username] %></label>
                  <input type="submit" value="Sign out" id="signout">
                </form>
              <% else %>
                <p>Sign in below</p>
              <% end %>
            </li>
          </ul>
        </nav>
      </div>
    </header>
    <main>
      <div class="inventory-backing">
        <div>
        <% if session[:msg] %>
          <span class="flash"><%= session.delete(:msg) %></span>
        <% end %>
        </div>

        <%== yield %>
      </div>
    </main>
  </body>
</html>